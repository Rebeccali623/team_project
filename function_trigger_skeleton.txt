DROP TRIGGER IF EXISTS verify_{} on {};
DROP FUNCTION IF EXISTS {};

CREATE FUNCTION {}()
RETURNS trigger AS $BODY$
BEGIN
IF new.{} {} {} THEN
    RETURN NEW;
ELSE 
    RAISE EXCEPTION 'Invalid {}';
END IF;
END;
$BODY$
LANGUAGE 'plpgsql';

CREATE TRIGGER verify_{} BEFORE INSERT OR UPDATE ON {}
            FOR EACH ROW EXECUTE PROCEDURE {}();